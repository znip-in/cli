import chalk from 'chalk';

export const red = text => chalk.red(text);
export const blue = text => chalk.blue(text);
export const green = text => chalk.green(text);

export const redBold = text => chalk.bold.red(text);
export const blueBold = text => chalk.bold.blue(text);
